#include <iostream>

using namespace std;
// int, long long, char, float and double
int main()
{
    long long a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << "Difference"
         << " = " << (a * b) - (c * d) << endl;
    return 0;
}